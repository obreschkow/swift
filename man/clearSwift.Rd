% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clearSwift.R
\name{clearSwift}
\alias{clearSwift}
\title{Clear variables from the `swift` environment}
\usage{
clearSwift(names = NULL, all.names = FALSE)
}
\arguments{
\item{names}{Character vector of object names to clear or remove. If \code{NULL}, all objects
in `swift` are targeted. Default variables are cleared (set to \code{NULL}) rather than removed.}

\item{all.names}{a logical value. If TRUE, all object names are removed. If FALSE, names which begin with a '.', such as `.paths`, are omitted.}
}
\value{
None. Modifies or removes entries from the `swift` environment and its internal storage.
}
\description{
Empties or removes variables stored in `swift` and its internal storage.
Default variables (active bindings like `simulation`, `halos`, `particles`) are retained but set to \code{NULL}.
}
